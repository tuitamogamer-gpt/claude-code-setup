# consolidate-memory

"Reflective pass over your memory files — merge duplicates, fix stale facts, prune the index."

## Install

This skill lives in your claude.ai account (Settings → Capabilities → Skills) and follows the account automatically. This copy is a backup; it can also be used in the CLI by copying the folder to `~/.claude/skills/consolidate-memory/`.
