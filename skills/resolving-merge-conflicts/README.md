# resolving-merge-conflicts

"Use when you need to resolve an in-progress git merge/rebase conflict."

## Install

Copy this folder to `~/.claude/skills/resolving-merge-conflicts/` on the target machine (or run `install.sh` from the repo root).
