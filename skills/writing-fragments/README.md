# writing-fragments

Writing, explore — mine raw fragments, no structure yet.

## Install

Copy this folder to `~/.claude/skills/writing-fragments/` on the target machine (or run `install.sh` from the repo root).
