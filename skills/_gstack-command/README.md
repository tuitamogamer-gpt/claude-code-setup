# _gstack-command

Router for the gstack skill suite. (gstack)

## Install

Copy this folder to `~/.claude/skills/_gstack-command/` on the target machine (or run `install.sh` from the repo root). Trigger inside Claude Code with `/_gstack-command`.
