# claude-handoff

Hand the current conversation off to a fresh background agent that picks up the work immediately.

## Install

Copy this folder to `~/.claude/skills/claude-handoff/` on the target machine (or run `install.sh` from the repo root).
