# understand-diff

Use when you need to analyze git diffs or pull requests to understand what changed, affected components, and risks

## Install

Copy this folder to `~/.claude/skills/understand-diff/` on the target machine (or run `install.sh` from the repo root). Trigger inside Claude Code with `/understand-diff`.
