# understand-explain

Use when you need a deep-dive explanation of a specific file, function, or module in the codebase

## Install

Copy this folder to `~/.claude/skills/understand-explain/` on the target machine (or run `install.sh` from the repo root). Trigger inside Claude Code with `/understand-explain`.
