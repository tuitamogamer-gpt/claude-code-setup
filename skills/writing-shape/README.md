# writing-shape

Writing, exploit — shape raw material into an article, paragraph by paragraph.

## Install

Copy this folder to `~/.claude/skills/writing-shape/` on the target machine (or run `install.sh` from the repo root).
