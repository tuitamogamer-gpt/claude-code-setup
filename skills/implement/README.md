# implement

"Implement a piece of work based on a spec or set of tickets."

## Install

Copy this folder to `~/.claude/skills/implement/` on the target machine (or run `install.sh` from the repo root).
