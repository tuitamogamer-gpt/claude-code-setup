# understand-dashboard

Launch the interactive web dashboard to visualize a codebase's knowledge graph

## Install

Copy this folder to `~/.claude/skills/understand-dashboard/` on the target machine (or run `install.sh` from the repo root). Trigger inside Claude Code with `/understand-dashboard`.
