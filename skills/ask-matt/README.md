# ask-matt

Ask which skill or flow fits your situation. A router over the skills in this repo.

## Install

Copy this folder to `~/.claude/skills/ask-matt/` on the target machine (or run `install.sh` from the repo root).
