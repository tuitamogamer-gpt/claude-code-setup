# understand-onboard

Use when you need to generate an onboarding guide for new team members joining a project

## Install

Copy this folder to `~/.claude/skills/understand-onboard/` on the target machine (or run `install.sh` from the repo root). Trigger inside Claude Code with `/understand-onboard`.
