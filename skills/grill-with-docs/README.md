# grill-with-docs

A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.

## Install

Copy this folder to `~/.claude/skills/grill-with-docs/` on the target machine (or run `install.sh` from the repo root).
