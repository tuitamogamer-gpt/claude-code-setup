# handoff

Compact the current conversation into a handoff document for another agent to pick up.

## Install

Copy this folder to `~/.claude/skills/handoff/` on the target machine (or run `install.sh` from the repo root).
