# writing-beats

Writing, exploit — assemble raw material into a journey of beats, grounding each term before a beat leans on it.

## Install

Copy this folder to `~/.claude/skills/writing-beats/` on the target machine (or run `install.sh` from the repo root).
