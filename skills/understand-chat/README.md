# understand-chat

Use when you need to ask questions about a codebase or understand code using a knowledge graph

## Install

Copy this folder to `~/.claude/skills/understand-chat/` on the target machine (or run `install.sh` from the repo root). Trigger inside Claude Code with `/understand-chat`.
